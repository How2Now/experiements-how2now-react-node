import { green, blue } from 'material-ui/colors';

/*
const theme = {
  palette: {
    primary: green,
    secondary: blue,
  },
};
*/

// export const appTheme = theme;

export default {
  palette: {
    primary: green,
    secondary: blue,
  },
};
